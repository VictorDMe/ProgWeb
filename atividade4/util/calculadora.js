module.exports = {
    somar: (a, b) => Number.parseInt(a) + Number.parseInt(b),
    subtrair: (a, b) => Number.parseInt(a) - Number.parseInt(b),
    multiplicar: (a, b) => Number.parseInt(a) * Number.parseInt(b),
    dividir: (a, b) => Number.parseInt(a) / Number.parseInt(b),
}